# 複製這個檔案為 .env.local（已在 .gitignore 的 *.local 規則內，不會被提交）
# 才能開啟「帳號與同步」功能。兩者缺一，這個功能會整個隱藏——
# 不接 Supabase 一樣拿到完整可用的純本地版本，見 src/stores/sync.ts 的 isConfigured。
#
# 到 https://supabase.com/dashboard 建專案後，在 Project Settings → API 找到這兩個值。
# ANON_KEY 設計上就是可以公開的，真正的存取控制是 supabase/migrations 裡的 RLS policy。
#
# 這兩個值只是接上 Supabase 專案本身；目前畫面上實際能用的登入方式是
# Google／GitHub 一鍵登入（信箱登入連結底層邏輯還在，但表單暫時隱藏，見
# src/components/AccountDialog.vue 的 EMAIL_LOGIN_ENABLED）。要開啟
# Google／GitHub，另外要在 Supabase Dashboard 的 Authentication → Providers
# 設定，步驟見 README.md 的「選配：接上跨裝置同步」。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 選配：Web Push 推播通知（被 @提及時跳通知，M4）。缺這個值時「推播
# 通知」的開關整個不顯示，不影響其他任何功能——多的是還沒接上這塊的
# 使用者，跟同步是不是接上完全獨立的兩件事。
#
# 用 `node scripts/generate-vapid-keys.mjs` 產生一組（每個部署環境各自
# 產生自己的，不要共用），公鑰貼在這裡；私鑰要貼進 Supabase Edge
# Function 的環境變數（supabase/functions/send-mention-push），不是這個
# 檔案——那個是機密，這裡只需要公鑰。
VITE_VAPID_PUBLIC_KEY=
